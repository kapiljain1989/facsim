# Minimal broker for the simulator's live feed. Anonymous access is fine for a
# local synthetic-data stream; do not use this as-is anywhere real.
listener 1883
allow_anonymous true
persistence false
log_dest stdout
connection_messages true
